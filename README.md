# volcano-tf-multiworkermirrored

```bash
gh auth login

gh repo create just-a-test \
    --public \
    --clone \
    --template https://github.com/rmrschub/volcano-tf-multiworkermirrored.git

cd just-a-test
git submodule init
git submodule update
```